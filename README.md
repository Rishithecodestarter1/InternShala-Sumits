# youtube-clone

A MERN stack YouTube clone capstone project with a React + Vite client and a Node.js + Express + MongoDB server.

The project is being rebuilt into this structure:

```text
youtube-clone/
  client/
  server/
  README.md
```
